<Window x:Class="evecorpfy.ViewsFornecedor.NegociacaoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Negociação de Serviços"
        Height="450" Width="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Cabeçalho -->
            <RowDefinition Height="*"/>
            <!-- Grid -->
            <RowDefinition Height="Auto"/>
            <!-- Rodapé -->
        </Grid.RowDefinitions>
        <!-- Cabeçalho -->
        <TextBlock Grid.Row="0"
                   Text="Serviços do Evento"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        <!-- Grid de Serviços -->
        <DataGrid x:Name="DataGridServicos"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10"
                  IsReadOnly="False" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Serviço" Binding="{Binding NomeServico}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Minha Proposta (R$)" Binding="{Binding ValorProposta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Rodapé -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <TextBlock Text="Orçamento Total: "
                       VerticalAlignment="Center"/>
            <TextBlock x:Name="TextBlockOrcamentoRestante"
                       Text="R$ 0,00"
                       FontWeight="Bold"
                       Margin="5,0,20,0"
                       VerticalAlignment="Center"/>
            <Separator Width="10" Opacity="0"/>
            <Button Content="Voltar"
                    Width="150"
                    Click="Voltar_Click"/>
            <Separator Width="10" Opacity="0"/>
            <Button Content="Enviar Orçamento"
                    Width="150"
                    Click="EnviarOrcamento_Click"/>
        </StackPanel>
    </Grid>
</Window>